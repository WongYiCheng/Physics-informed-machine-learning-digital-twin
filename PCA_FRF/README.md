This folder have only 1 code to process FRF to PCA-FRF with given range. There are total of 4 output files namely, frf_data, PCA_frf data, the others two can be ignored.
Cell 3 purposes is to prepare a new set of PCA_frf where the original set of data is being replcaed with a new set of FEA synthetic data.
